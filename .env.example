# Optional: override the Python executable (defaults to .venv, then python).
# FORMA_PYTHON=C:\path\to\python.exe
# Provider credentials are read by the server-side Forma process only.
# OPENAI_API_KEY=
# LLM_PROVIDER=openai
# LLM_MODEL=your-model-id
PORT=8787
